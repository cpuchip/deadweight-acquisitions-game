node_modules
dist
.playwright-cli
*.log
npm-debug.log*
rooms-state.json
# NOTE: .git is intentionally NOT ignored — the build reads the commit hash from it
# (git rev-parse) to stamp the version; the Dockerfile removes it after building.
